import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Comprobantes de compra - Celagem",
  description: "Comprobantes de compra - Celagem",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}