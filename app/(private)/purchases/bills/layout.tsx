import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Facturas de compra - Celagem",
  description: "Facturas de compra - Celagem",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}