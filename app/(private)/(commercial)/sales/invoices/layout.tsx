import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Comprobantes de venta - Celagem",
  description: "Comprobantes de venta - Celagem",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}